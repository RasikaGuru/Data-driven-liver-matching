Readme file for executing the Code:

#This application is built on Python Flask backend.

1)The templates folder has the HTML and CSS files for rendering the pages on the application
2)input_file folder has the test CSV files which can be uploaded for testing.
3)main.py needs to be executed first. The local URL where the application is hosted will be displayed on the console after execution.
4)Click the URL and follow the steps in the User Manual for the web application.
